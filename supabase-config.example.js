// Copy this file to `supabase-config.js` and replace the placeholders with
// the values from your Supabase project settings. Keep `supabase-config.js`
// out of version control (add to .gitignore) if you don't want to publish keys.

window.SUPABASE_CONFIG = {
    url: 'https://YOUR-PROJECT.supabase.co',
    anonKey: 'public-anon-key-goes-here'
};
